# Decision Memory Engine (DME) — Python dependencies
# Install with:  pip install -r requirements.txt

streamlit>=1.30        # dashboard UI
scikit-learn>=1.3      # RandomForest model, StratifiedKFold, metrics
pandas>=2.0            # data handling
numpy>=1.24            # numerics
matplotlib>=3.7        # charts / evaluation plots
joblib>=1.3            # model (de)serialization (.pkl)
groq>=0.11             # Groq LLM client (Llama 3.3 70B) — AI tabs

# Note: sqlite3 is part of the Python standard library (no install needed).
# imbalanced-learn is only needed if you re-run SMOTE during model training:
# imbalanced-learn>=0.11
